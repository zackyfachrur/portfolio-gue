export type ProjectType = {
  title: string;
  type: string;
  role: string;
  year: number;
  category: Array<string>;
  thumbnail: Array<string>;
}

export type PresentIconType = {
  image: string;
}